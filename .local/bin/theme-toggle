#!/usr/bin/env bash
# Flip the desktop between light and dark based on the current XDG scheme.
set -euo pipefail

current=$(gsettings get org.gnome.desktop.interface color-scheme)
case "$current" in
  *prefer-dark*) exec theme-apply light ;;
  *)             exec theme-apply dark  ;;
esac
